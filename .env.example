# Client Hub - Environment Variables
# Copy to .env and fill in values: cp .env.example .env

# Database credentials for the shared MariaDB instance (~/docker/mariadb/)
# The clienthub_db database and user must be created there first.
DB_USER=clienthub
DB_PASSWORD=changeme_app_password
